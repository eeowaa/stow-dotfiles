Most files here are no longer relevant but might be useful for reference.
I'm guessing that a majority of them were workarounds and hacks for Cygwin.
A few other things (e.g. `.local/opt/logparse`) just need some cleanup.
