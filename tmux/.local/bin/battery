#!/bin/sh
## Display the current battery percentage (or "??%")
{
    case `uname -s` in
    Linux)  acpi -b ;;
    Darwin) pmset -g batt ;;
    *)      false ;;
    esac 2>/dev/null || echo '??%'
} | sed -n 's/.*[ 	]\([0123456789?]\{1,3\}%\).*/\1/p'
